# CORS settings
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://sendbyte.net
CORS_ALLOW_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$

# Paths (relative paths are resolved from the project root)
UI_DIST_DIR=dist
PUBLIC_ASSETS_DIR=assets
UPLOAD_DIR=files
